{
  "name": "nuxt-seo-meta-base",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "yarn workspace next-seo-meta jest",
    "dev": "yarn workspace next-seo-meta nuxt dev",
    "build": "yarn workspace next-seo-meta siroc build && mkdist --src packages/core/src/runtime --dist dist/runtime"
  },
  "devDependencies": {
    "prettier": "^2.2.1",
    "typescript": "^4.1.3",
    "ts-loader": "^8.0.12",
    "eslint": "^7.16.0",
    "eslint-config-prettier": "^8.0.0",
    "@typescript-eslint/eslint-plugin": "^4.15.2",
    "@typescript-eslint/parser": "^4.15.2",
    "@nrwl/eslint-plugin-nx": "^11.3.1"
  }
}
